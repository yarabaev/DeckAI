dist
# Generated by `pnpm codegen` — formatting is owned by the codegen
# script (compact JSON, schema-driven markdown, HTML). Prettier should
# not reflow it, otherwise codegen:check vs fmt:check fight each other.
dist-schema
reference-html
builder.xsd
builder.schema.json
reference.md
# Golden snapshots written by `src/codegen/html.test.ts` — the test
# asserts byte equality against the codegen output, so prettier
# reformatting these files would make every test fail.
src/codegen/__snapshots__
